see where you can apply {props.children}
react profiler
crashes after shuffling with repeat

https://webpack.js.org/guides/code-splitting/
https://webpack.js.org/guides/lazy-loading/
https://github.com/webpack/analyse
dynamic imports
webpack build optimization

only load samplers when they are needed
hiding parts should silence those parts in playback
separate mongodb test environment

ornaments not always deactivated when in generate link modal.
    -generate link
    -click copy
    -close modal
    -generate another link (ornaments will not be disabled)

message indicating supported browsers
some sort of licensing system to prevent cloning environment and making another site
tooltips on hover
mutate context resets when adding a note